spring:
  datasource:
    # Soru işaretine kadar olan temiz URL
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres
    # Supabase'in verdiği yeni kullanıcı adı
    username: postgres.dtrqehgokyldwswyddvs
    # Kendi veritabanı şifren (Köşeli parantezler olmadan yazmalısın)
    password: qqhLJukSssJ81yiq
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true